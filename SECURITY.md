# 安全策略

## 支持的版本

| 版本 | 支持情况 |
|------|----------|
| 0.9.x | ✅ 安全修复 |
| < 0.9.0 | ❌ 不再维护 |

## 报告安全漏洞

如果你发现了安全漏洞，请**不要**在公开 Issue 中披露。

请通过以下方式私下报告：

1. 使用 GitHub 的 [私密安全报告功能](https://github.com/xkun1/ai-devtoolkit/security/advisories/new)
2. 或发送邮件至仓库所有者（见 GitHub 主页）

我们会在 **72 小时内**确认收到，并在 **7 天内**给出初步评估。

### 报告时请包含

- 漏洞类型（如 SSRF、路径逃逸、命令注入、信息泄露等）
- 影响范围与利用条件
- 复现步骤或 PoC
- 建议的修复方案（可选）

## 安全设计说明

devtoolkit 涉及网络抓取、本地文件读写和脚本生成，我们在以下位置做了防护：

- **SSRF 防护**：远程文档抓取仅允许公网 HTTP(S)，DNS 解析后固定连接已校验的公网 IP，防止 DNS rebinding（见 `src/utils/safe-fetch.ts`）
- **路径逃逸**：Web UI 固定项目根目录边界，使用 `realpath` 校验，阻止符号链接逃逸（见 `src/server/validation.ts`）
- **命令注入**：环境快照导入采用无 Shell 的参数化执行，拒绝换行注入（见 `src/env/importer.ts`）
- **敏感信息**：环境探测输出自动脱敏 token/password/apikey，导出文件使用 0600/0700 最小权限
- **内容上限**：远程抓取限制响应体大小、超时与重定向次数，MCP 与 LLM 调用均有 Token 预算

## 已知边界

- LLM 提炼路径的输出内容取决于所用模型，请勿将生成的技能包视为可信代码直接执行
- `--env-import` 生成的安装脚本包含包管理器命令，请在导入前人工审查
